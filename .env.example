# Vercel → Settings → Environment Variables

# Pi Network (server-only)
PI_API_KEY=your_pi_server_api_key

# Supabase
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: public anon key if you add direct client reads later
# SUPABASE_ANON_KEY=your_anon_key

# Pi SDK — Testnet MVP: sandbox true in pi-payment.js
# App ID is configured in develop.pi portal (domain-linked, not in code)
